{
    "scripts": {
        "manifest": "node scripts/generate-asset-manifest.mjs",
        "dev": "vite --port 5173",
        "build": "vite build",
        "preview": "vite preview --port 4173"
    },
    "dependencies": {
        "express": "^5.2.1"
    },
    "devDependencies": {
        "vite": "^8.0.10"
    }
}

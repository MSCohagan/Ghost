#!/usr/bin/env bash
set -e

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
start_file="$repo_root/start"

[ -f "$start_file" ] || exit 0
sed -i 's/\r$//' "$start_file"
chmod +x "$start_file" || true
